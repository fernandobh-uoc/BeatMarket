import { ArticleCharacteristicsTranslatePipe } from './article-characteristics-translate.pipe';

describe('ArticleCharacteristicsTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleCharacteristicsTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
