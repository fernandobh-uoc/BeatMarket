import { TranslateFilterValuePipe } from './translate-filter-value.pipe';

describe('TranslateFilterValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateFilterValuePipe();
    expect(pipe).toBeTruthy();
  });
});
