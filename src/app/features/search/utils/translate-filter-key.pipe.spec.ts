import { TranslateFilterKeyPipe } from './translate-filter-key.pipe';

describe('TranslateFilterKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateFilterKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
